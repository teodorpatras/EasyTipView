# references:
# * http://www.objc.io/issue-6/travis-ci.html
# * https://github.com/supermarin/xcpretty#usage

language: objective-c
osx_image: xcode8
env:
  global:
    - WORKSPACE=EasyTipView.xcworkspace
    - FRAMEWORK_NAME=EasyTipView
    - PROJECT=EasyTipView.xcodeproj
    - SCHEME=EasyTipView
install:
- gem install xcpretty --no-rdoc --no-ri --no-document --quiet
before_deploy:
  - carthage build --no-skip-current
  - carthage archive $FRAMEWORK_NAME
script:
- set -o pipefail
- xcodebuild -workspace $WORKSPACE -scheme $SCHEME -destination 'platform=iOS Simulator,name=iPhone 6' clean test | xcpretty -c
- pod lib lint --quick
